# Railway deployment configuration
spring.application.name=multi-level-market

# Database configuration - will be overridden by Railway environment variables
spring.datasource.url=${DATABASE_URL:jdbc:mysql://root:eRpmnhKpeqRMBETxYXgLktlnrdvLgZDJ@tramway.proxy.rlwy.net:23898/railway}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:eRpmnhKpeqRMBETxYXgLktlnrdvLgZDJ}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Server configuration
server.port=${PORT:8081}

# JPA configuration
spring.jpa.show-sql=false
spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Database connection resilience
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000

# Email configuration - will be overridden by Railway environment variables
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:rahulmekala22@gmail.com}
spring.mail.password=${MAIL_PASSWORD:nufy kvuk srfe wces}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Swagger configuration
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.path=/swagger-ui.html

# Actuator configuration
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized
management.health.db.enabled=true
management.health.defaults.enabled=true

# Health check endpoint for Railway
management.endpoint.health.show-components=always
management.health.livenessstate.enabled=true
management.health.readinessstate.enabled=true
